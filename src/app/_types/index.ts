export interface RawBookItemType {
  itemId: string | number
  cover?: string
  title?: string
  author?: string
}

export interface BookType {
  id: string | number
  cover: string
  title: string
  author: string
}
